name: example
description: example of functional data

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  functional_data:

dev_dependencies:
  build_runner:
  build_resolvers:
  functional_data_generator:
